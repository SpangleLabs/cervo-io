{
  "name": "common-lib",
  "version": "0.0.0",
  "description": "Common library for anything shared between frontend and backend of cervo.io",
  "main": "apiInterfaces.js",
  "scripts": {
    "test": "echo \"Error: no tests for common lib\" && exit 1"
  }
}
